# Database Health Report — 2026-06-07

## Environment
- DB: staging (vaultmtg_staging) + prod roles queried via separate tunnel
- Connection method: SSM port forward (i-0226bf51fcf09b506 → staging RDS port 5454, prod RDS port 15432)
- Credentials: Secrets Manager (app/staging/db-secret-arn, app/production/db-secret-arn)

## pg_stat_statements
Extension `pg_stat_statements` is available but NOT installed (`installed_version` is null). Slowest-query data unavailable. Recommend enabling for future waves — file a Pam ticket.

## Index Usage Issues

Tables with seq_scan dominating idx_scan (staging):

| Table | seq_scan | idx_scan | idx_usage_pct |
|---|---|---|---|
| daemon_api_keys | 41,150 | 156 | 0.4% |
| accounts | 12,885 | 48 | 0.4% |
| users | 30,197 | 9,751 | 24.4% |
| matches | 2,530 | 1,188 | 32.0% |
| decks | 1,529 | 350 | 18.6% |
| match_game_results | 912 | 464 | 33.7% |
| draft_sessions | 900 | 876 | 49.3% |
| quests | 582 | 88 | 13.1% |

`daemon_api_keys` and `accounts` have near-zero index usage (0.4%) with very high seq_scan counts — likely being full-table scanned on every API key lookup and account resolution. These warrant index review tickets.

## Table Sizes (staging)

| Table | Total Size |
|---|---|
| set_cards | 26 MB |
| daemon_events | 9.3 MB |
| draft_card_ratings | 1.8 MB |
| matches | 256 kB |
| decks | 224 kB |
| game_plays | 216 kB |
| draft_sessions | 200 kB |
| draft_picks | 192 kB |
| quests | 176 kB |
| card_combination_stats | 144 kB |

No tables near the 10 GB archival threshold. All within expected ranges for staging.

## Role Inventory (ticket #971 context)

### Staging
| Role | can_login | notes |
|---|---|---|
| mtga_admin_staging | yes | migration runner, ROLECREATE/DBCREATE |
| vaultmtg_staging_app | yes | app role, owns all tables on staging |
| mtga_sync | yes | sync lambda role |

### Production
| Role | can_login | notes |
|---|---|---|
| mtga_admin | yes | master/DDL role, owns all prod tables |
| vaultmtg_app | yes | app role |
| vaultmtg_staging_app | yes | present in prod (staging app role) |
| mtga_sync | yes | sync lambda role |

**Key finding for #971:** Prod master DDL role = `mtga_admin`. `ALTER DEFAULT PRIVILEGES FOR ROLE mtga_admin` is the correct anchor for future-table SELECT coverage. Staging DDL role = `vaultmtg_staging_app`.

## Recommendations

1. **File index ticket (Pam):** `daemon_api_keys` — 0.4% idx usage at 41K seq_scans. Likely missing index on `account_id` or `key` column used in API key lookup hot path.
2. **File index ticket (Pam):** `accounts` — 0.4% idx usage at 12K seq_scans. Review what columns are used in WHERE clauses.
3. **File extension ticket (Pam):** Enable `pg_stat_statements` on staging RDS (requires parameter group change + reboot). Needed for mean_exec_time analysis.
